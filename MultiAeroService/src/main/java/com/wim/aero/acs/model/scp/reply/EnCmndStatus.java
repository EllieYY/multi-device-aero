package com.wim.aero.acs.model.scp.reply;

public enum EnCmndStatus {
    // - 0 = FAILED (could not send, SCP off-line)
    // - 1 = OK (delivered and accepted),
    // - 2 = NAK'd (command rejected by the SCP)
}

